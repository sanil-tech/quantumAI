export class AccountService { public static resolveAccountId(reqAccountId?: string): string { const candidate = (reqAccountId || '').trim(); if (candidate && candidate.length > 0 && candidate !== 'undefined' && candidate !== 'null' && /^[a-zA-Z0-9_-]+$/.test(candidate)) { return candidate; } const envAccountId = (process.env.CTRADER_ACCOUNT_ID || process.env.DEFAULT_ACCOUNT_ID || '').trim(); if (envAccountId && envAccountId.length > 0 && /^[a-zA-Z0-9_-]+$/.test(envAccountId)) { return envAccountId; } throw new Error('ACCOUNT_RESOLUTION_FAILED: No valid accountId supplied or configured in server environment.'); } }
